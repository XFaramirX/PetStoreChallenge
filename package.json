{
  "name": "api-petstore",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generateReport": "newman run src/petstorecollection.json  -r htmlextra --reporter-htmlextra-darkTheme",
    "test": "newman run src/petstorecollection.json",
    "build": "npm run test && npm run generateReport && node main.js",
    "docker": "docker run -t postman/newman run https://www.getpostman.com/collections/ ",
    "dockerReport": "docker-compose up && node main.js && git commit -am 'Updated Report' && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "newman": "^4.6.0",
    "newman-reporter-htmlextra": "^1.12.1"
  }
}
