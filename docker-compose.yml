version: '2'
services:
  postman_checks:
    container_name: petstorecollection
    build: .
    image: postman_checks
    command: run ./src/petstorecollection.json
      -r htmlextra --reporter-htmlextra-darkTheme
    volumes:
      - ./:/etc/newman
